apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monblog-ingress
  namespace: sans-mvc
spec:
  rules:
  - host: sansmvc.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: monblog-service
            port:
              number: 80
