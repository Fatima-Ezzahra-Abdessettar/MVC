apiVersion: apps/v1
kind: Deployment
metadata:
  name: monblog
  namespace: sans-mvc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monblog
  template:
    metadata:
      labels:
        app: monblog
    spec:
      containers:
      - name: web
        image: monblog:sans-mvc
        imagePullPolicy: Never
        ports:
        - containerPort: 80
